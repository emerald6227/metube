extends base.pug
include mixins/video

block content
    div#home-container
        include partials/navigation
        div.empty-box
        div.video-grid
            each info in videos
                +video(info)
            else
                span.empty__message 업로드된 동영상이 없습니다

block scripts
    script(src="/static/js/previewVideo.js")