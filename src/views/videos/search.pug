extends ../base.pug
include ../mixins/video

block content 
    div.default__container
        div.video-grid.video-grid--small
            each video in videos 
                +video(video)
            else
                span.empty__message 조건에 맞는 동영상이 없습니다

block scripts 
    script(src="/static/js/previewVideo.js")